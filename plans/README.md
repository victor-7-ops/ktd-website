# Implementation Plans

Generated by the improve skill on 2026-06-11 (audit at commit `780d398`).
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Contact form reports real success/failure | P1 | S | — | DONE |
| 002 | Eliminate dead `#` links (real URLs or disabled states) | P1 | S | — | DONE |
| 003 | SEO foundation: metadataBase, OG image, sitemap, robots, JSON-LD | P1 | M | — | DONE |
| 004 | Fix stale CLAUDE.md version, add .env.example | P2 | S | — | DONE |
| 005 | Accessibility batch: mobile menu, carousel, cursor | P2 | M | 002 (soft — Social.tsx overlap) | DONE |
| 006 | Playwright smoke-test baseline | P2 | M | — | DONE |
| 007 | Defer Three.js chunk on desktop video hero | P3 | M | 006 (hard) | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 007 requires 006: the hero is the most visible element on the site; the smoke
  suite must exist (and pass before/after) to gate the change.
- 005 should run after 002: both touch `components/sections/Social.tsx`
  (disabled-card rendering). 005's Step 4 is skipped if 002 is DONE.
- Plans 001–005 each name an optional Playwright assertion to add once 006 lands.

## Findings considered and rejected

- **`npm audit` moderate advisories (postcss via next)**: transitive pin inside
  `next` itself; the only "fix" is a breaking downgrade. Wait for a Next patch.
- **Index-as-key in Shows/Media lists**: static, never-reordered data — harmless.
- **`overflow-x: hidden` on body**: intentional, part of the grain/cursor design.

## Direction findings (not planned — maintainer decisions)

- **EPK PDF**: `components/sections/Contact.tsx` links `/ktd-epk.pdf` (TODO in
  code); the file does not exist in `public/`, so the press button 404s.
  Produce the PDF or hide the card.
- **Media assets**: `components/sections/Media.tsx` has a labeled YouTube embed
  slot awaiting a video ID; all photos are placeholders. Section is built —
  only assets missing.
- **Merch commerce**: `Merch.tsx` TODO offers Big Cartel vs Shopify;
  `NEXT_PUBLIC_BIG_CARTEL_SHOP` env var documented but unread by code. Needs a
  decision before any wiring; a spike plan can be requested via
  `/improve plan <decision>`.
